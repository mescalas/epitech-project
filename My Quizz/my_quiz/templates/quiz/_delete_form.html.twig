<form method="post" action="{{ path('quiz_delete', {'id': q.id ?? quiz.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ q.id ?? quiz.id) }}">
    <button class="mt-4 p-0.5 rounded-lg border border-gray-400 bg-gradient-to-b from-white to-gray-200">Delete</button>
</form>
